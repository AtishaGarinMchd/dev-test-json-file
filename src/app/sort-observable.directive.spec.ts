import { SortObservableDirective } from './sort-observable.directive';

describe('SortObservableDirective', () => {
  it('should create an instance', () => {
    const directive = new SortObservableDirective();
    expect(directive).toBeTruthy();
  });
});
