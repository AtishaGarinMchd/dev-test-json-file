import { SortUnderscoreDirective } from './sort-underscore.directive';

describe('SortUnderscoreDirective', () => {
  it('should create an instance', () => {
    const directive = new SortUnderscoreDirective();
    expect(directive).toBeTruthy();
  });
});
