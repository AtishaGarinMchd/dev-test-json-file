import { SortVanillaDirective } from './sort-vanilla.directive';

describe('SortVanillaDirective', () => {
  it('should create an instance', () => {
    const directive = new SortVanillaDirective();
    expect(directive).toBeTruthy();
  });
});
